name: Apply LC32 Bootstrap Lookup Patch

on:
  pull_request:
    paths:
      - 'tools/apply-lc32-bootstrap-lookup.py'
      - '.github/workflows/apply-lc32-bootstrap-lookup.yml'

permissions:
  contents: write

jobs:
  apply-bootstrap-lookup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: feature/liveexec32-fixitfelix
      - run: python3 tools/apply-lc32-bootstrap-lookup.py
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add runtime/LC32DarwinSyscalls.cpp runtime/tests/LC32DarwinSyscallsTests.cpp
          git commit -m 'Add scoped bootstrap lookup MIG replies'
          git push origin HEAD:feature/liveexec32-fixitfelix
