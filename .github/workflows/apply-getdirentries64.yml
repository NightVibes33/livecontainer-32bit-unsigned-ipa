name: Apply LC32 getdirentries64 Patch

on:
  pull_request:
    paths:
      - 'tools/apply_getdirentries64.py'
      - '.github/workflows/apply-getdirentries64.yml'

permissions:
  contents: read

jobs:
  apply-getdirentries64-patch:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - name: Apply getdirentries64 runtime patch
        run: python3 tools/apply_getdirentries64.py
      - name: Commit runtime and tests
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add runtime/LC32DarwinSyscalls.hpp runtime/LC32DarwinSyscalls.cpp runtime/tests/LC32DarwinSyscallsTests.cpp
          git commit -m "runtime: add getdirentries64 support"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
