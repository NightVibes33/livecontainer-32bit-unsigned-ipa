name: Apply LC32 Bootstrap Port Patch

on:
  pull_request:
    paths:
      - 'tools/apply-lc32-bootstrap-port.py'
      - '.github/workflows/apply-lc32-bootstrap-port.yml'

permissions:
  contents: write

jobs:
  apply-bootstrap-port:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: feature/liveexec32-fixitfelix
      - run: python3 tools/apply-lc32-bootstrap-port.py
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add runtime/LC32DarwinSyscalls.cpp runtime/tests/LC32DarwinSyscallsTests.cpp
          git commit -m 'Add task bootstrap special-port MIG reply'
          git push origin HEAD:feature/liveexec32-fixitfelix
